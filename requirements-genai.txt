# Installed with --no-deps to avoid websockets version conflict:
#   alpaca-trade-api requires websockets<11
#   google-genai requires websockets>=13  →  irreconcilable conflict
# We only use REST generate_content(), not streaming, so skipping websockets is safe.
google-genai==0.3.0
