# google-genai (current active SDK, requires websockets>=13)
# alpaca-py requires websockets>=10.4 (no upper cap) → COMPATIBLE!
# No more --no-deps workaround needed since switching from alpaca-trade-api.
google-genai
