# numpy, pandas, PyYAML, requests, pytz → installed via apt (pre-compiled for ARM32)
# All remaining packages below → installed via pip + PiWheels

feedparser==6.0.12
python-dotenv==1.2.1
openai>=1.12.0
httpx<0.28.0
alpaca-trade-api==3.1.1
# google-genai 0.2.2: last version before websockets>=13 dep was added (alpaca needs websockets<11)
google-genai==0.2.2
pandas_market_calendars==4.4.1
pytz==2024.1
